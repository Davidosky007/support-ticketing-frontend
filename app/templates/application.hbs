{{page-title "Support Ticketing System"}}

<div class="app-container">
  <header class="navbar">
    <div class="navbar-container">
      <div class="navbar-brand">
        <a href="/" class="logo">Support Ticketing</a>
      </div>
      
      <nav class="navbar-menu">
        {{#if this.session.isAuthenticated}}
          {{#if this.session.isAgent}}
            <a href="/agent/dashboard" class="nav-item">Dashboard</a>
            <a href="/agent/tickets" class="nav-item">Tickets</a>
          {{else}}
            <a href="/customer/tickets" class="nav-item">My Tickets</a>
            <a href="/customer/new-ticket" class="nav-item">New Ticket</a>
          {{/if}}
          <button class="nav-item btn-logout" {{on "click" this.logout}}>Logout</button>
        {{else}}
          <a href="/login" class="nav-item btn-login">Login</a>
          <a href="/register" class="nav-item btn-signup">Sign Up</a>
        {{/if}}
      </nav>
    </div>
  </header>

  <main>
    {{outlet}}
  </main>

  <footer class="footer">
    <div class="container">
      <p>© {{this.currentYear}} Support Ticketing System</p>
    </div>
  </footer>
</div>